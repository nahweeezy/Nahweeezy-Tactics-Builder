# Local environment
.env
.env.local
.vercel
.DS_Store
*.log

# Build artifacts & caches
node_modules
dist
.vite

# Local editor / tooling config — machine-specific paths, not shared
.vscode/
.idea/

# Large 3D model — convert to .glb and re-add (~10–20 MB) before pushing to GitHub
public/assets/models/stadium.dae
public/assets/models/textures/
